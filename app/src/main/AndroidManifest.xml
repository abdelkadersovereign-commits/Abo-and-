name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: 8.1
        cache-disabled: true

    # ✅ الخطأ الثاني: كتابة GEMINI_API_KEY من GitHub Secret إلى local.properties
    - name: Write local.properties
      run: |
        echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> local.properties

    # ✅ الخطأ الثالث: منح صلاحية التنفيذ لـ gradlew
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build with Gradle
      run: ./gradlew assembleDebug

    - name: Print SHA-1 fingerprint
      if: always()
      run: |
        if [ -f "$HOME/.android/debug.keystore" ]; then
          keytool -list -v \
            -keystore "$HOME/.android/debug.keystore" \
            -alias androiddebugkey \
            -storepass android \
            -keypass android | grep -A2 "Certificate fingerprints"
        else
          mkdir -p "$HOME/.android"
          keytool -genkeypair \
            -v \
            -keystore "$HOME/.android/debug.keystore" \
            -alias androiddebugkey \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass android \
            -keypass android \
            -dname "CN=Android Debug,O=Android,C=US"
          keytool -list -v \
            -keystore "$HOME/.android/debug.keystore" \
            -alias androiddebugkey \
            -storepass android \
            -keypass android | grep -A2 "Certificate fingerprints"
        fi

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: asyria-sentinel-debug
        path: app/build/outputs/apk/debug/app-debug.apk
